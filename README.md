# EuropassXML_to_ReactiveResumeJSON
A Python utility that converts Europass Candidate XML files into app-compatible Reactive Resume JSON, preserving CV content while reusing template metadata and layout defaults.
